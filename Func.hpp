#pragma once
#include <math.h>
#include <fstream>
#include <iostream>

namespace Masiv
{
    int* FunInput(int &a, int Mess[10000]);

    void FunOutput(int a, int Mass[]);

    int* FunSort(int &a, int Mess[10000]);
}

